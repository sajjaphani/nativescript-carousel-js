const { slide: slide1 } = require('./slide1');
const { slide: slide2 } = require('./slide2');
const { slide: slide3 } = require('./slide3');

const slides = [slide1, slide2, slide3];

module.exports = slides